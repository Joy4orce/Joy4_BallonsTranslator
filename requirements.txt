# To install pytorch cuda (gpu) version, please look https://pytorch.org/

PyQt6-Qt6==6.4.2    # for qt>6.4.2 , framelesswindow is broken on windows, and text rendering is wrong on macOS
PyQt6==6.4.2   #PyQt5>=5.15.9
numpy
urllib3==1.25.11 # https://github.com/psf/requests/issues/5740
jaconv
torch
torchvision
transformers
# optimum 2.x moved ONNX Runtime support out into `optimum-onnx`; the import
# path `optimum.onnxruntime` still works once that is installed. 1.x is not an
# option any more: 1.27 is the last of that line and it imports
# `_attention_scale` from torch.onnx.symbolic_opset14, which newer torch has
# removed, so it fails at import under the cu128 torch below.
#
# Note neither is installed with the [onnxruntime] extra — that pulls in the CPU
# `onnxruntime` package, which would clash with `onnxruntime-gpu` (same import
# name, same site-packages dir). Only the GPU build goes in; it provides
# CPUExecutionProvider as a fallback too.
optimum>=2.0
optimum-onnx
# ORT-GPU has no CUDA of its own on Windows: it borrows the DLLs that torch
# bundles (see modules/ocr/manga_ocr.py:_ensure_torch_dll_dir). So its CUDA
# generation has to match whatever torch is installed — the 1.17.x pin that
# used to be here went with the cu118 torch below. Blackwell (RTX 50xx, sm_120)
# needs CUDA 12.8, hence cu128 torch and an ORT built against CUDA 12.
onnxruntime-gpu>=1.22; sys_platform == 'win32'
onnxruntime-gpu>=1.22; sys_platform == 'linux'
# Mac has no NVIDIA — install the CPU package there.
onnxruntime; sys_platform == 'darwin'
fugashi
unidic_lite
tqdm
opencv-python
shapely
pyclipper
einops
termcolor
bs4
deepl>=1.16.0
qtpy
spacy-pkuseg
sentencepiece
ctranslate2
python-docx
docx2txt
piexif
keyboard
ordered-set
opencc-python-reimplemented
requests
Pillow
beautifulsoup4
colorama
openai
pyyaml
natsort
py7zr
multivolumefile
httpx[socks,brotli]
langdetect
pywin32; sys_platform == 'win32'
winsdk; sys_platform == 'win32'
brotlicffi; sys_platform == 'win32'
pyobjc-core; sys_platform == 'darwin'
pyobjc-framework-cocoa; sys_platform == 'darwin'
pyobjc-framework-coreml; sys_platform == 'darwin'
pyobjc-framework-quartz; sys_platform == 'darwin'
pyobjc-framework-vision; sys_platform == 'darwin'
